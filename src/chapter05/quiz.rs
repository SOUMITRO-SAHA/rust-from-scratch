/// Quiz 1: Define a struct called Book with fields title (String) and pages (u32).
pub struct Book {
    // TODO: Add fields
}

/// Quiz 2: Implement a method on Book that returns true if pages > 300.
impl Book {
    pub fn is_long(&self) -> bool {
        // TODO: Implement
        false
    }
}

/// Quiz 3: Create a tuple struct for a 2D point (x, y: i32).
pub struct Point(i32, i32);
