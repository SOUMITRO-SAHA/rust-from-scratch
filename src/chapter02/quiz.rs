/// Quiz 1: Make a mutable variable, set it to 5, then change it to 10. Return the value.
pub fn mutable_var() -> i32 {
    // TODO: Replace the code below
    let x = 0;
    x
}

/// Quiz 2: Parse the string "42" into an integer and return it.
pub fn parse_42() -> i32 {
    // TODO: Use parse and expect
    0
}

/// Quiz 3: Write a function that takes a string and returns its length.
pub fn string_length(s: &str) -> usize {
    // TODO: Return the length of the string
    0
}

